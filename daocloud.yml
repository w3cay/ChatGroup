version: 2.0
test:
    image:
        node:4

    services:
        - mongodb

    # using default docker-link env
    env:

    install:

    before_script:
        - npm install

    script:
        - npm run start
  build:
    image:
        dockerfile_path: Dockerfile
        build_dir: /
        cache: false