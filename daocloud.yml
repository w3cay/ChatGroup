version: 2.0
test:
    image:
        node:4

    services:
        - mongodb

    # using default docker-link env
    env:

    install:
        - yarn

    before_script:
        - yarn

    script:
        - yarn start
  build:
    lite_image:
        compile:
            dockerfile_path: Dockerfile
            build_dir: /
            cache: false

        extract:
            - /go/bin/app

        package:
              dockerfile_path: Dockerfile.sec
              build_dir: /
              cache: false
